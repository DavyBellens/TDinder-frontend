/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [{
      protocol: 'https',
      hostname: "1ablevltxd9i7fjm.public.blob.vercel-storage.com"
    }]
  }
}